# ============================================================================
# PIXFLOW - Environment Variables Configuration
# ============================================================================
# Copy este arquivo para .env.local (NUNCA commitar .env com valores reais!)
# ============================================================================

# BACKEND (Flask)
# ============================================================================

# Banco de dados PostgreSQL (produção) ou SQLite (desenvolvimento)
# Exemplos:
#   Dev SQLite:  sqlite:///pixflow.db
#   Prod PostgreSQL: postgresql://user:password@host:5432/pixflow
DATABASE_URL=sqlite:///pixflow.db

# JWT - String aleatória para assinar tokens (usar: openssl rand -hex 32)
JWT_SECRET=your-super-secret-key-change-this-in-production

# Encryption - Para criptografia Fernet (usar: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
ENCRYPTION_KEY=your-fernet-encryption-key-change-this

# Mercado Pago (DEPRECATED - agora por usuário em /api/settings/mp)
# Manter vazio ou fallback apenas para migrações
MERCADO_PAGO_ACCESS_TOKEN=

# Frontend URL - Para CORS restrito a origem confiável
FRONTEND_URL=http://localhost:5173

# Admin inicial
ADMIN_EMAIL=admin@pixflow.local
ADMIN_PASSWORD=ChangeMe123!

# Flask environment
FLASK_ENV=development


# FRONTEND (React/Vite)
# ============================================================================

# URL base do backend (construído durante build com Vercel)
# Dev: Automaticamente proxied via vite.config.js
# Produção: Configurado no vercel.json ou Vercel dashboard
VITE_API_BASE=http://localhost:5000
